🚀 How to run everything

From inside your dotfiles repo:

chmod +x install.sh install/\*.sh
./install.sh

That will:

Install all your dev tools (this new script)

Set up zsh + starship + plugins

Apply TokyoNight themes to tmux, Ghostty, LazyVim

Copy configs into ~/.config
